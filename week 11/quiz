1. 다른 어플리케이션에서 현재 프로그램의 기능을 사용할 수 있게 허용하는 접점을 ( API  )라고 한다.

2. JWT는 (  JSON   ) 형식의 데이터를 저장하는 토큰이다.

3. JWT 토큰은(  JWT 비밀 키키  )를 알지 않는 이상 변조가 불가능하고, 변조한 토큰은 (   시그니처
4.  API 사용하는 서비스는 다른 서버에 요청을 보내므로 (   클라이언트트  ) 역할을 한다.

5. (    apiLimiter    )미들웨어를 라우터에 넣으면 라우터에 사용량 제한이 걸린다.

6. 현재 요청을 보내는 클라이언트와 요청을 받는 서버의 도메인이 달라 발생하는 문제를 (   CORS   )문제라고 한다.

7. (  credential:true   ) 옵션은 Access-Control-Allow-Credentials 헤더를 true로 만든다.