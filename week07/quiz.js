/*
1. http 모듈의 요청과 응답 객체에 추가 기능을 부여한 것은 (  익스프레스   )라 한다.  

2. 요청과 응답의 중간에 위치하는 익스프레스의 핵심을 (  미들웨어  )라 한다.

3. 에러 처리 미들웨어는 (  err  ), (  req  ), (  res ), (  next  )로 네 개의 매개변수를 갖는다.

4. 라우터에 연결된 나머지 미들웨어를 건너뛰고 싶다면 (   next()   )를 사용한다.

5. 익스프레스의 res, req 객체는 (  http   )모듈의 req, res 객체를 확장한 것이다.  

6. (  템플릿 엔진  )을 이용하면 자바스크립트를 사용해 HTML을 간단히 처리해 렌더링할 수 있다.

7. 에러 처리 미들웨어에서 스택 트레이스는 시스템 환경이 (   배포환경   )이 아닌 경우 표시된다.
*/