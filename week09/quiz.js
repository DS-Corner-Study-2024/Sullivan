/* 1. 라우터 마지막에 위치해 클라이언트에 응답을 보내는 미들웨어를 (  컨트롤러   ) 라고 한다.

​

2. User 모델과 Post 모델은 (  1:N  ) 관계에 있고, 팔로잉 기능은 (   N:M  ) 관계에 있다.

​

3. 회원 가입과 로그인을 구현할 때 사용하는 모듈은 (   Passport   ) 모듈이다.

​

4. (   multer   ) 패키지는 이미지를 서버 디스크에 저장한다.

​

5. 서버는 요청에 반드시 응답해야하며, 여러 번 응답할 수 있다. [ X ]

​

6. (   dotenv   ) 패키지와 (  .env    ) 파일로 유출되면 안 되는 비밀 키를 관리해야 한다.

​

7. 규모가 큰 프로젝트를 관리하기 위해 라우터는 (  routes    ) 폴더에, 데이터베이스는 (   modules   ) 폴더에, html 파일은 (  views    ) 폴더에 구분해서 저장한다.
*/